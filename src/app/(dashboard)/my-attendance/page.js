/**
 * Shared My Attendance page — accessible by all roles.
 * All sidebar "My Attendance" links across every module point here (/my-attendance).
 * This simply re-exports the HR my-attendance page so it's one single source of truth.
 */
export { default } from "@/app/(dashboard)/hr/my-attendance/page";
